## 2.5 ER MODEL

erDiagram

tb_deposit_interest_contract ||--o{ tb_deposit_interest_quota_usage : "has"

tb_deposit_interest_contract {

bigserial id pk "流水號"

varchar(16) contract_no uk "議價編號"

date transaction_date "訂約日期"

date value_date "起息日期"

date expire_date "到期日期"

varchar(6) branch_class "分行類型"

varchar(4) branch_code "分行代碼"

varchar(4) bsn_unit "績效行"

varchar(35) customer_reference_no "客戶識別號"

varchar(1) is_stakeholder "金控利害關係人"

decimal(10) product_reference "產品別"

varchar(3) currency "幣別"

decimal(3) interest_type "利率別"

decimal(3) period_unit_1 "期間單位1"

decimal(6) period_value_1 "期間值1"

decimal(3) period_unit_2 "期間單位2"

decimal(6) period_value_2 "期間值2"

decimal(11-7) ftp_rate "ftp利率"

decimal(11-7) dealing_rate "承作利率"

decimal(24-7) dealing_amount "承作金額"

varchar(80) remarks "註記"

varchar(35) userid_op "交易櫃員"

varchar(35) userid_verify "覆核主管"

timestamp datetime_verify "覆核日期"

varchar(10) contract_status "合約狀態"

varchar(3) contract_type "合約類型"

varchar(100) created_by "建立者"

timestamp created_timestamp "建立時間"

varchar(100) updated_by "最後更新者"

timestamp updated_timestamp "最後更新時間"

varchar(100) created_by_user "建立業務使用者"

varchar(100) updated_by_user "最後更新業務使用者"

}

tb_deposit_interest_quota_usage {

bigserial id pk "流水號"

varchar(30) uuid pk "識別碼"

varchar(16) contract_no fk "議價編號"

decimal(24-7) amount "交易金額"

date transaction_date "額度交易日期"

varchar(7) quota_transaction_status "明細狀態"

varchar(35) userid_op "交易櫃員"

varchar(35) userid_verify "覆核主管"

timestamp datetime_verify "覆核日期"

varchar(100) created_by "建立者"

timestamp created_timestamp "建立時間"

varchar(100) updated_by "最後更新者"

timestamp updated_timestamp "最後更新時間"

varchar(100) created_by_user "建立業務使用者"

varchar(100) updated_by_user "最後更新業務使用者"

}

tb_customer_branch_setting {

bigserial id pk "流水號"

varchar(3) currency "幣別"

decimal(24-7) customer_daily_max "客戶額度金額上限"

decimal(24-7) branch_single_max "分行單筆承作上限"

decimal(24-7) branch_daily_max "分行當日承作上限"

varchar(100) created_by "建立者"

timestamp created_timestamp "建立時間"

varchar(100) updated_by "最後更新者"

timestamp updated_timestamp "最後更新時間"

varchar(100) created_by_user "建立業務使用者"

varchar(100) updated_by_user "最後更新業務使用者"

}

tb_ftp_range_setting {

bigserial id pk "流水號"

varchar(3) currency "幣別"

decimal(11-7) ftp_sub_max "ftp利率減碼下限"

decimal(11-7) ftp_add_max "ftp利率加碼上限"

varchar(100) created_by "建立者"

timestamp created_timestamp "建立時間"

varchar(100) updated_by "最後更新者"

timestamp updated_timestamp "最後更新時間"

varchar(100) created_by_user "建立業務使用者"

varchar(100) updated_by_user "最後更新業務使用者"

}

tb_max_interest_setting {

bigserial id pk "流水號"

varchar(3) currency "幣別"

decimal(3) period_unit "期間單位"

decimal(6) period_value_min "期間值下限"

decimal(6) period_value_max "期間值上限"

decimal(11-7) max_rate "最高利率"

varchar(100) created_by "建立者"

timestamp created_timestamp "建立時間"

varchar(100) updated_by "最後更新者"

timestamp updated_timestamp "最後更新時間"

varchar(100) created_by_user "建立業務使用者"

varchar(100) updated_by_user "最後更新業務使用者"

}